<% header(name + ' 0.6 Release Notes') %>

<p>Webware for Python 0.6</p>

<a name="ReleaseNotes"></a><h2>Release Notes</h2>

<ul>
	<li> New Application.config setting ClearPSPCacheOnStart: When set to zero, the app server will allow PSP instances to persist from one AppServer run to the next.  If you have PSPs that take a long time to compile, this can give a speedup.</li>
</ul>

<% footer() %>